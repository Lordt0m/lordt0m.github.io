# Use a static progressively enhanced site

The portfolio will use semantic HTML and CSS, with JavaScript added only for a proven accessible interaction. A framework, CMS, template engine, and client-side content renderer would increase the knowledge, dependency, and verification surface without improving the first release's job: moving a recruiter from positioning to inspectable evidence quickly. Reconsider this decision only after real repeated pages or content operations make the static public document a maintenance problem.

